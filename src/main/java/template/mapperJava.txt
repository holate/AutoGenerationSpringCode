package ${packages}.dao;

import ${packages}.entity.${tableNameJava?cap_first};
import org.springframework.stereotype.Repository;
import java.util.List;

/**
 * Created on ${createTime}.
 */
@Repository
public interface ${tableNameJava?cap_first}Dao {

    /**
     * 增加
     *
     * @param ${tableNameJava}
     * @return
     */
    Integer insertSelective(${tableNameJava?cap_first} ${tableNameJava});

    /**
     * 删除
     *
     * @param id
     * @return
     */
    Integer deleteByPrimaryKey(Integer id);

    /**
     * 更新
     *
     * @param ${tableNameJava}
     * @return
     */
    Integer updateByPrimaryKeySelective(${tableNameJava?cap_first} ${tableNameJava});

    /**
     * 根据id查询
     *
     * @param id
     * @return
     */
    ${tableNameJava?cap_first} selectByPrimaryKey(Integer id);
}
