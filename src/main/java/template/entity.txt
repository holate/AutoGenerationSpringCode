package ${packages}.entity;

import lombok.Data;
import lombok.Builder;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;

import java.util.Date;
/**
 * Created on ${createTime}.
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ${tableNameJava?cap_first} {

<#list fieldList as fields>
    /**
     * ${fields.remark}
     */
    private ${fields.type} ${fields.javaName};

</#list>
}
