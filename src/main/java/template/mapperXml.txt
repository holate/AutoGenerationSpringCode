<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${packages}.dao.${tableNameJava?cap_first}Dao">

    <sql id="Base_Column_List">
        <trim suffixOverrides=",">
        <#list fieldList as fields>
                `${fields.sqlName}`,
        </#list>
        </trim>
    </sql>

    <insert id="insertSelective" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT into ${tableNameSql}
        <trim prefix="(" suffix=")" suffixOverrides=",">
        <#list fieldList as fields>
            <if test="${fields.javaName} != null">`${fields.sqlName}`,</if>
        </#list>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
        <#list fieldList as fields>
            <if test="${fields.javaName} != null">${'#{'}${fields.javaName}${'}'},</if>
        </#list>
        </trim>
    </insert>

    <delete id="deleteByPrimaryKey">
        UPDATE ${tableNameSql} SET status_flag = 0 where id = ${'#{'}id${'}'}
    </delete>

    <update id="updateByPrimaryKeySelective">
        UPDATE ${tableNameSql}
        <set>
        <#list fieldList as fields>
            <if test="${fields.javaName} != null">`${fields.sqlName}`=${'#{'}${fields.javaName}${'}'},</if>
        </#list>
        </set>
        WHERE id = ${'#{'}id${'}'}
    </update>

    <select id="selectByPrimaryKey" resultType="${packages}.entity.${tableNameJava?cap_first}">
        select
        <include refid="Base_Column_List"/>
        from ${tableNameSql}
        where id = ${'#{'}id${'}'}
    </select>

</mapper>